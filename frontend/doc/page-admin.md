# 管理员后台

- 登录：`/admin/login`；后台前缀：`/admin/*`；实现位于 `src/pages/Admin/`。
- 使用独立的管理员令牌和 `/api/v1/admin-auth/*`，不复用普通用户 SSO token。
- 导航覆盖运营概览、项目用户、素材治理、创作资产、质量巡检、专属积分、运维观测和系统维护。
- 后台只管理 AINovel 本地边界，不提供外部服务的账号、模型、密钥、通用账务或全局告警配置。
