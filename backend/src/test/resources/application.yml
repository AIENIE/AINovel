spring:
  mvc:
    servlet:
      path: /api
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
  data:
    redis:
      host: localhost
      port: 0
      password:

server:
  port: 0

app:
  jwt:
    secret: test-secret-change-me-1234567890abcdef-test-secret-change-me-1234567890
    expiration-minutes: 120

sso:
  session-validation:
    enabled: false
